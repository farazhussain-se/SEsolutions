{
  "name": "staffbase-page-pdf-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": { "@cloudflare/puppeteer": "^1.1.0" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
